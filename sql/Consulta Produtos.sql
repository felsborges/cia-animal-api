-- A palavra chave SELECT representa as colunas
--   que serão selecionadas, aonde '*' representa todas
--   as colunas
SELECT nome
-- O FROM representa a origem da informação, desta forma
--   deve-se informar a tabela que contém a informação
FROM produtos